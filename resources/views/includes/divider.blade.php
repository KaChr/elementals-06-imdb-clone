<div class="divider">
    <div class="divider__info is-flex">
        <h5 class="divider__title">{{ $title }}</h5>
        @if(!isset($link))
            <a href="#">VIEW ALL</a>
        @endif
    </div>
    <span class="divider__line"></span>
</div>